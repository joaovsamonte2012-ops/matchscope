name: Gerar APK MatchScore Beta

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow legado desativado. Use build-apk-v23.yml."
